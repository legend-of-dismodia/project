{% extends 'base.html.twig' %}
{% block title %}Inscription - {{ parent() }}{% endblock %}

{% block body %}
    <h1 class="mt-3">Création de compte</h1>

    {{ form_start (form, {
            'attr': {'novalidate': 'novalidate'}
        }) }}
    {{ form_widget (form) }}

    <button class="btn btn-primary" type="submit">Créer</button>
    {{ form_end (form) }}


{% endblock %}